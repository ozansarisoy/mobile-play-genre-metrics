# Test-only dependencies — not needed to run the app itself, only to run
# the automated test suite (tests/test_smoke.py). Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
pdfplumber>=0.11
